#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

struct node{
		int data;
		int priority;
		struct node *next;
};

int main(void)
{	
	struct node *new_node;
	new_node=malloc(sizeof(struct node));
  
  
}